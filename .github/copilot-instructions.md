# Workspace instructions

- Keep the React frontend in the frontend folder and the FastAPI backend in the backend folder.
- Use the Supabase database URL from backend/.env for database access.
- Keep the SQL schema in sql/schema.sql for creating tables in Supabase.
